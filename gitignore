# --- WindFoil .gitignore (v1.0.0) ---

# Secrets — NEVER commit
windfoil.env
.env
.env.*
!*.env.example

# Database (holds password hashes + user/session data) and its WAL sidecars
data/
*.db
*.db-wal
*.db-shm
*.sqlite

# Dependencies
node_modules/

# Built artifacts (deploy.sh regenerates these)
public/vendor/auth-client.bundle.js

# Logs / OS noise
*.log
.DS_Store
