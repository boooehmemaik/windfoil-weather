{
  "name": "windfoil-station-proxy",
  "version": "3.8.10",
  "description": "WindFoil Weather Intelligence — backend server with auth, feedback loop and SQLite persistence.",
  "main": "proxy-server.js",
  "scripts": {
    "start": "node proxy-server.js"
  },
  "dependencies": {
    "better-auth": "^1.6.18",
    "better-sqlite3": "^12.10.1",
    "cors": "^2.8.5",
    "esbuild": "^0.28.1",
    "express": "^4.22.2",
    "node-fetch": "^2.7.0"
  },
  "engines": {
    "node": ">=18"
  }
}
